import { TextLengthsPipe } from './text-lengths-pipe';

describe('TextLengthsPipe', () => {
  it('create an instance', () => {
    const pipe = new TextLengthsPipe();
    expect(pipe).toBeTruthy();
  });
});
